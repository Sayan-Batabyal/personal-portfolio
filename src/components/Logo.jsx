import React from 'react'

const Logo = ({className=""}) => {
  return (
    <div>
        <h1 className={` ${className} select-none  text-2xl  font-roboto `} >myPortfolio.</h1>
    </div>
  )
}

export default Logo